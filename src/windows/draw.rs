extern crate winapi;

use winapi::shared::windef::HDC;
use winapi::shared::windef::RECT;

pub fn draw(hdc: HDC, rect: &RECT) {
    // Ajoutez ici le code pour dessiner dans le contexte de périphérique (HDC)
    // et la zone de rectangle (RECT)
}
